# basic-usage

## Usage

To install dependencies:

```bash
bun install
```

To run:

This will start QuestDB and expose the HTTP API on port `9000` (used by the sample) and then run the example.

```bash
bun dev
```

To stop QuestDB when you're done:

```bash
bun run takedown
```
