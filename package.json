{
  "name": "questdb-typesafe-client-client-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "fmt": "turbo run fmt",
    "test": "turbo run test"
  },
  "workspaces": [
    "docs",
    "packages/**",
    "samples/**"
  ],
  "packageManager": "bun@1.3.9",
  "devDependencies": {
    "oxfmt": "^0.32.0",
    "turbo": "^2.8.7"
  }
}
